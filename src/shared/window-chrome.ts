// Shared chrome heights for renderer topbars, Win/Linux titleBarOverlay, and
// macOS traffic-light centering. Keep native chrome and renderer layout on
// shared constants so future tweaks do not drift across process boundaries.
export const WINDOW_TOPBAR_HEIGHT_PX = 34
export const APP_TOOLBAR_HEIGHT_PX = 34
